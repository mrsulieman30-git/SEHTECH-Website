version: '3.8'

services:
  web:
    build: .
    ports:
      - "5173:5173"
      - "3001:3001"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
